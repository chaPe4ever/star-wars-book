# A Vanilla JS Project

### Makes ue of SWAPI to get info regarding the starwars movies, starships and pilots. Uses sass for styling and defining mixins and vars. Uses vanilla JS to create and manipulate the html elements and styling
